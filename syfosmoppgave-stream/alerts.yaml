apiVersion: "nais.io/v1"
kind: "Alert"
metadata:
  name: syfosmoppgave-stream-alerts
  namespace: teamsykmelding
  labels:
    team: teamsykmelding
spec:
  receivers:
    slack:
      channel: '#sykmelding-alerts'
      #prependText: '<!here> | '
  alerts:
    - alert: syfosmoppgave-stream er nede
      expr: up{app="syfosmoppgave-stream", job="kubernetes-pods"} == 0
      for: 5m
      description: "App {{ $labels.app }} er nede i namespace {{ $labels.kubernetes_namespace }}"
      action: "`kubectl describe pod {{ $labels.kubernetes_pod_name }} -n {{ $labels.kubernetes_namespace }}` for events, og `kubectl logs {{ $labels.kubernetes_pod_name }} -n {{ $labels.kubernetes_namespace }}` for logger"
    - alert: syfosmoppgave-stream-kafka-offset
      expr: kafka_consumergroup_group_sum_lag{group=~"teamsykmelding.syfosmoppgave-stream_stream_"} > 100
      for: 1m
      description: "syfosmoppgave-stream kafka-lag er høyt"
      action: "Sjekk om syfosmoppgave-stream leser fra kafka"
      sla: respond within 1h, during office hours
      severity: danger